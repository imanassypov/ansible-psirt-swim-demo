---
# =============================================================================
# swim / preflight.yml  —  backward-compatible alias for validate_compliance.yml
# =============================================================================

- name: Run validate compliance (preflight alias)
  ansible.builtin.import_tasks: validate_compliance.yml
