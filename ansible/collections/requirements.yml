---
collections:
  # SWIM workflow manager — the module playbooks 00.2, 01.1–01.3, and 02.1 drive
  - name: cisco.catalystcenter
    version: "2.9.0"
  # nginx image server (stage 00.1) uses community.general.ufw
  - name: community.general
    # 11.0.0+ requires ansible-core >= 2.18; this project pins core to 2.17.
    version: ">=8.0.0,<11.0.0"
